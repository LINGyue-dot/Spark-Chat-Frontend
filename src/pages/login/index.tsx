import React from 'react';
import styled from 'styled-components';

const Login = () => {
  return <FormDiv>qweewq</FormDiv>;
};

const FormDiv = styled.div`
  height: 200px;
  width: 200px;
  backgroundcolor: pink;
`;

export default Login;
